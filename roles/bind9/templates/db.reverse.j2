$TTL 604800
@ IN SOA {{ ansible_fqdn }}. root.{{ REALM.lower() }}. (
2011091301 ; Serial
604800 ; Refresh
86400 ; Retry
2419200 ; Expire
604800 ) ; Negative Cache TTL
	IN	NS	{{ ansible_fqdn }}.
{{ ansible_host.split('.')[3] }}	IN	PTR	{{ ansible_fqdn }}.
